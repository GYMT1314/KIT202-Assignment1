<!DOCTYPE html>
<html>
  <head>
      <title>Welcome to register a new account!</title>
  </head>
  <div class="register-content" style="top: 86px;">...<div/>
   <form action="" method="post" enctype="multipart/form-data" name="reg_form">
    <div class="info_input">
        <div class="ptag">Username：</div>
        <div class="ftag"><input name="userName" type="text" class="inputstyle" maxlength="80" /></div>
    </div>
    <div class="info_input">
        <div class="ptag">Password：</div>
        <div class="ftag"><input id="usersPw" type="password" name="userPw" class="inputstyle" minlength="8" maxlength="16" /></div>
    </div>
    <div class="info_input">
        <div class="ptag">Confirm Password：</div>
        <div class="ftag"><input id="confirmPw" type="password" name="confirmPw" class="inputstyle" minlength="8" maxlength="16" /></div>
        <script>
    function check() {
        var password = document.getElementById("userPw");
        var password = document.getElementById("confirmPw");
        if (usersPw.value != confirmPw.value) {
            alert("The two password entries are different, please re-enter");
        } else if (userPw.value != "" || confirmPw.value != "") {
            alert("Invalid password, please re-enter")
        } else {
            location.href = "success.html"
        }
    }
</script>
    </div>
    <div class="info_input">
        <div class="ptag"> Gender：</div>
        <div class="ftag">
            <input type="radio" name="gender" id="radio" value="male" />Male
            <input type="radio" name="gender" id="radio" value="female" />Female
            <input name="gender" type="radio" id="radio" value="secrecy" checked="checked" />Secret
        </div>
        <div class="info_input">
            <div class="ptag">Avatar：</div>
            <div class="ftag">
                <input name="fileField" type="file" id="fileField" size="60" />
            </div>
        </div>
        <div class="info_btn">
            <input name="submitbtn" type="submit" class="btnstyle" id="button" value="Sign up now" />
            <input name="resetbtn" type="reset" class="btnstyle" id="button" value="Unregister" />
        </div>
        
        //I really don’t know how to pop up a reminder to tell the user that the two passwords entered are different, 
          how to increase the strength of the password, 
          and how to center the table.
